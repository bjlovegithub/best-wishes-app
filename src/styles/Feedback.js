import React, {StyleSheet} from 'react-native';

export default StyleSheet.create({
  backgroundStyle : {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: 'white',
    marginBottom: 50,
  }
});
